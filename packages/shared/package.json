{
    "name": "shared",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "build": "rm -rf dist && bun run tsc"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
