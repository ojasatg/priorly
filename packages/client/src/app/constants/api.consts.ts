const USER_SIGNUP = "/api/auth/signup";
const USER_LOGIN = "/api/auth/login";

const GET_ALL_TOODS = "/api/todo/all";

const APIs = {
    USER_SIGNUP,
    USER_LOGIN,

    GET_ALL_TOODS,
};

export default APIs;
